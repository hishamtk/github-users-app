import { createMuiTheme } from "@material-ui/core";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#1A535C",
    },
    secondary: {
      main: "#197278",
    },
    background: {
      main: "#d8f3dc",
    },
    
  },
});

export default theme;
